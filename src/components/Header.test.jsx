import { describe, it, expect, test, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import Header from './Header';

describe('Test that Converted integer value is decimal', () => {
	it('int is divided by bigger number which result is a decimal', () => {
		expect(Convert(int)).toBe(float);
	});
});
